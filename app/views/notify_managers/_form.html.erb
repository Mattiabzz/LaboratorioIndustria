<%= form_with(model: notify_manager) do |form| %>
  <% if notify_manager.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(notify_manager.errors.count, "error") %> prohibited this notify_manager from being saved:</h2>

      <ul>
        <% notify_manager.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :tipo, style: "display: block" %>
    <%= form.text_area :tipo %>
  </div>

  <div>
    <%= form.label :manager_id, style: "display: block" %>
    <%= form.text_field :manager_id %>
  </div>

  <div>
    <%= form.label :event_id, style: "display: block" %>
    <%= form.text_field :event_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
